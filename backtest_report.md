# Adaptive BIST Orderflow Meta-Engine V1

Backtest reports are generated by `backtest_optimizer.py` only from real historical data. If the remote historical data source is unavailable or insufficient, the job fails closed instead of generating synthetic market data.
